{
  "name": "functions",
  "version": "1.0.0",
  "description": "Genkit flows for NIA Safety Assistant",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "private": true,
  "dependencies": {
    "@genkit-ai/firebase": "^1.14.1",
    "@genkit-ai/googleai": "^1.14.1",
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.0.2",
    "genkit": "^1.14.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}